diff --git a/docs/native-libraries.rst b/docs/native-libraries.rst
index f377592..6d9855e 100644
--- a/docs/native-libraries.rst
+++ b/docs/native-libraries.rst
@@ -287,7 +287,7 @@ with copies of ``libgfortran``, ``libquadmath`` and ``libgcc_s``:
    >>> print_tree(dylibs_directory)
    .dylibs/
      libgcc_s.1.dylib
-     libgfortran.4.dylib
+     libgfortran.3.dylib
      libquadmath.0.dylib
 
 For example, the ``_curve_speedup`` module depends on the local copy
@@ -322,7 +322,7 @@ of ``libgfortran``:
    >>> invoke_shell('otool', '-L', '_curve_speedup.cpython-36m-darwin.so')
    $ otool -L _curve_speedup.cpython-36m-darwin.so
    _curve_speedup.cpython-36m-darwin.so:
-           @loader_path/.dylibs/libgfortran.4.dylib (...)
+           @loader_path/.dylibs/libgfortran.3.dylib (...)
            /usr/lib/libSystem.B.dylib (...)
 
 Though the Python extension modules (``.so`` files) only depend on
@@ -333,10 +333,10 @@ Though the Python extension modules (``.so`` files) only depend on
    :options: +NORMALIZE_WHITESPACE
    :mac-os-x-only:
 
-   >>> invoke_shell('otool', '-L', '.dylibs/libgfortran.4.dylib')
-   $ otool -L .dylibs/libgfortran.4.dylib
-   .dylibs/libgfortran.4.dylib:
-           /DLC/bezier/libgfortran.4.dylib (...)
+   >>> invoke_shell('otool', '-L', '.dylibs/libgfortran.3.dylib')
+   $ otool -L .dylibs/libgfortran.3.dylib
+   .dylibs/libgfortran.3.dylib:
+           /DLC/bezier/libgfortran.3.dylib (...)
            @loader_path/libquadmath.0.dylib (...)
            /usr/lib/libSystem.B.dylib (...)
            @loader_path/libgcc_s.1.dylib (...)
