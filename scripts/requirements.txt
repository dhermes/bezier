# NumPy is required for building `bezier` from source.
numpy >= 1.17.0
# SciPy is an optional dependency for pure-Python implementations.
scipy >= 1.3.0
# Test requirements.
mock >= 3.0.5; python_version == "2.7"
pytest >= 5.0.1
